interface TYPE_DOMAIN {
  tokenId: number;
  name: string;
  owner: string;
  lastSalePrice?: number;
  price: number;
  collection: string;
  tags: string[];
  registedAt?: Date;
  expiresAt?: Date;
}
export type { TYPE_DOMAIN };
